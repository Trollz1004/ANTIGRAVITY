# Contracts — Historical On-Chain Artifacts

This folder contains historical contract code and deployment material for the already-observed Base Mainnet artifacts.

Important:
- do not treat files here as proof of current LLC operating doctrine
- do not market current live revenue as `60/30/10`, `100% charity`, or `100% DAO` based on this folder alone
- use `C:\ANTIGRAVITY\briefings\HISTORICAL-ONCHAIN-STATUS.md` for historical chain context
- use `C:\ANTIGRAVITY\briefings\CURRENT-REVENUE-LEGAL-CONSTRAINTS.md` and `C:\ANTIGRAVITY\briefings\PRELAUNCH-TAX-ADJUSTMENT-2026-03-31.md` for current doctrine
- the repo's current operating rule is a `1-wallet model with 10% reserve` (founder-directed), not an automatic gross-receipts smart-contract split
- no new on-chain revenue router should be deployed for current LLC commerce unless a future canonical legal review explicitly approves a matching structure
- `scripts/deploy.js` and `scripts/deploy-charity.js` are historical-use-only and require explicit opt-in environment variables before they will run
