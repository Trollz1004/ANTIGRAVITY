{
  "name": "antigravity-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "node scripts/deploy.js --check",
    "deploy": "node scripts/deploy.js",
    "verify": "node scripts/deploy.js --check"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0"
  }
}
