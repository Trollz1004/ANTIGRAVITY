name: gemini
version: 0.1
source: auth-signin
description: "Google Gemini — auth sign-in via Google AI Studio / gcloud. Uses `GEMINI_API_KEY` env after OAuth consent; no hardcoded credentials."
owner: sabretooth
node_id: sabretooth
cli_command: "gemini"
health_check: "gemini --version"
paperclip_alias: "gemini"
opencode_provider: "google"
opencode_model: "gemini-2.5-pro"
notes: "Gemini requires Google auth sign-in or API key from AI Studio. The opencode/google provider reads GEMINI_API_KEY."
