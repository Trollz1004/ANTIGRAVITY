name: pi
version: 0.2
source: local-cli
description: "Pi runtime — conversational / explanatory tasks. Uses Ollama local and OpenRouter free-tier fallbacks. No Anthropic."
owner: sabretooth
node_id: sabretooth
cli_command: "pi"
health_check: "pi --version"
paperclip_adapter_type: "pi_local"
paperclip_adapter_config:
  cwd: "C:\\antigravity"
  model: "openrouter/free"
paperclip_alias: "pi"
opencode_provider: "openrouter"
opencode_model: "openrouter/free"
notes: "Uses `pi_local` Paperclip adapter type. Falls back to Ollama local and OpenRouter free models. Shared OpenRouter key with Hermes/OpenCode."
