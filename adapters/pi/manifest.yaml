name: pi
version: 0.1
source: local-cli
description: "Pi runtime — conversational / explanatory tasks. Uses local Ollama and OpenRouter free-tier fallbacks. No Anthropic."
owner: sabretooth
node_id: sabretooth
cli_command: "pi"
health_check: "pi --version"
paperclip_alias: "pi"
opencode_provider: "openrouter"
opencode_model: "openrouter/free"
notes: "Pi is a local conversational runtime. Falls back to Ollama local and OpenRouter free models. Shared OpenRouter key with Hermes/Opencode."
