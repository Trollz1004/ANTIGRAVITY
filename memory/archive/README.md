# Memory Archive

This folder holds older one-off memory snapshots and local backup notes that do not need to stay in the top-level `memory/` view.

Rules:
- Do not put secrets here.
- Keep active memory files in the top-level `memory/` folder.
- Archive preserves history; it does not replace canonical current-state files.
