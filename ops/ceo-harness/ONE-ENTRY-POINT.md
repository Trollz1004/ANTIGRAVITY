# ONE ENTRY POINT — read this first, every session

CEO agent: start here. This is the whole map. Don't read the rest unless the task needs it — that's the point of keeping context short.

1. `SOUL.md` — who you are, what you never do. Read once, rarely changes.
2. `STATE.md` — what happened last session. Read every time. Keep it short.
3. `HEARTBEAT.md` — checks to run this session. Do them.
4. `NODES.md` — node inventory (Sabretooth, T5500, 9020, Laptop). Load only if the task touches infra.
5. `ORNITH-INSTALL.md` — copy/paste runbook for installing the Ornith 9B floor model on a node. Load only when doing that specific task.
6. `SKILLS-INDEX.md` — skill discipline (2 loaded, rest on demand). Load if you're about to reach for a skill.

Don't load all six every session. Read this file + `STATE.md`, do the `HEARTBEAT.md` checks, then pull in only what the actual task needs. That's the token-savings contract Josh set (caveman compression, short context, protect the 90%+ prompt cache hit rate).
