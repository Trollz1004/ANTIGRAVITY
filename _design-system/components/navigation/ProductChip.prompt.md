Tinted product pill for the hero row that introduces the ecosystem's surfaces.

```jsx
import { Heart, Handshake, Recycle, Coins } from 'lucide-react';
<ProductChip tone="pink" icon={<Heart size={15} />}>Dating & Community</ProductChip>
<ProductChip tone="success" icon={<Handshake size={15} />}>Business Exchange</ProductChip>
<ProductChip tone="purple" icon={<Coins size={15} />}>Product structure — Legal Review</ProductChip>
```

Each ecosystem surface has a conventional tone — pink (dating), success (marketplace), amber (support), purple (Product structure/paused).
