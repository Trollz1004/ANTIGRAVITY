# CEO Skills — Paperclip Pi Runtime

Use these skill domains when tasks match them. Read the skill file when needed in a live Pi session.

## Paperclip Skills

- `paperclip` — required for issue checkout, comments, status updates, delegation, routines, approvals.
- `paperclip-create-agent` — use for creating or configuring agents with governance-aware payloads.
- `paperclip-create-plugin` — use for Paperclip plugin scaffolding or plugin SDK work.

## Antigravity Mission Skills

- `mission-control` — mission board, Paperweight/Kanban status, routines, adapter health, 100% verified state reports.
- `payments` — Square/Stripe boundaries, checkout links, payment buttons, subscriptions, webhook verification.
- `revenue-model` — 1 LLC / 1 wallet / 10% per-bucket ; replaces legacy  and 100% claims.

## Memory / Database Skills

- `para-memory-files` — durable file memory, daily notes, plans, knowledge graph.
- `supabase` — only for Supabase Auth/DB/Storage/Edge/Realtime work.
- `supabase-postgres-best-practices` — only for Postgres schema/query/security/performance work.

## CEO Skill Priorities

1. Triage work and assign the right agent/company.
2. Keep Paperclip alive and agents routable.
3. Keep public surfaces compliant with current revenue/legal doctrine.
4. Route paid/high-value thinking across OpenAI Codex and Grok instead of tying all companies to one model.
5. Use Ollama cloud/local and OpenRouter/OpenCode only as fallback capacity lanes.
6. Protect secrets and never paste credential values.
7. Convert prototype assets into production tasks only after verifying they match canonical repo doctrine.

## Model Distribution Policy

Across four active companies, do not bind every CEO/agent to one model. Spread lanes:

- OpenAI Codex / `openai-codex/gpt-5.5`: top-level CEO, architecture, final synthesis.
- Grok / `xai/grok-4.20-0309-reasoning` or `xai/grok-4.4`: strategy, adversarial review, social/current-event tasks.
- Ollama cloud/local: routine execution, low-cost drafting, status checks.
- OpenRouter/OpenCode free models: overflow and non-critical work.
- Claude third-party adapters: avoid because of current usage constraints; do not rely on Claude API inside Paperclip unless Josh explicitly restores it.
