import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Activity, ArrowUpRight, BadgeCheck, Boxes, CircleAlert, ClipboardCheck, ShieldCheck } from "lucide-react";
import { trpc } from "@/lib/trpc";
import { Link } from "wouter";

const metricCards = [
  { key: "catalogCount", label: "Catalog records", detail: "Canonical product facts", icon: Boxes, tone: "text-sky-600 bg-sky-50" },
  { key: "verifiedCatalogCount", label: "Verified records", detail: "Eligible for a payload", icon: BadgeCheck, tone: "text-emerald-600 bg-emerald-50" },
  { key: "openApprovals", label: "Approval queue", detail: "Explicit decisions pending", icon: ClipboardCheck, tone: "text-violet-600 bg-violet-50" },
  { key: "openExceptions", label: "Open exceptions", detail: "Needs accountable resolution", icon: CircleAlert, tone: "text-amber-600 bg-amber-50" },
] as const;

export default function Home() {
  const overview = trpc.operations.overview.useQuery();
  const channels = trpc.operations.channels.useQuery();
  const data = overview.data ?? { catalogCount: 0, verifiedCatalogCount: 0, openApprovals: 0, openExceptions: 0, recentActivity: [] };
  return <DashboardLayout><div className="mx-auto flex w-full max-w-7xl flex-col gap-8 pb-12"><section className="relative overflow-hidden rounded-3xl bg-slate-950 px-6 py-8 text-slate-50 shadow-xl sm:px-9 sm:py-10"><div className="absolute inset-0 bg-[radial-gradient(circle_at_top_right,rgba(45,212,191,0.18),transparent_38%),radial-gradient(circle_at_20%_100%,rgba(59,130,246,0.18),transparent_35%)]"/><div className="relative max-w-3xl"><Badge className="bg-white/10 text-slate-100 hover:bg-white/10">LLC operations control room</Badge><h1 className="mt-5 text-3xl font-semibold tracking-tight sm:text-4xl">One catalog. Controlled channel workflows.</h1><p className="mt-4 max-w-2xl text-base leading-7 text-slate-300">Manage verified product facts, inventory availability, approval-gated marketplace work, scoped automation profiles, and a complete operational record from one internal workspace.</p><div className="mt-7 flex flex-wrap gap-3"><Link href="/catalog"><Button className="bg-teal-300 text-slate-950 hover:bg-teal-200">Open catalog <ArrowUpRight className="ml-2 h-4 w-4"/></Button></Link><Link href="/listings"><Button variant="outline" className="border-white/20 bg-white/5 text-white hover:bg-white/10 hover:text-white">Review listing desk</Button></Link></div></div></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metricCards.map(card => <div key={card.key} className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className={`flex h-10 w-10 items-center justify-center rounded-xl ${card.tone}`}><card.icon className="h-5 w-5"/></div><p className="mt-5 text-3xl font-semibold tracking-tight">{data[card.key]}</p><p className="mt-1 text-sm font-medium">{card.label}</p><p className="mt-1 text-xs text-muted-foreground">{card.detail}</p></div>)}</section><section className="grid gap-5 lg:grid-cols-[1.2fr_0.8fr]"><div className="rounded-2xl border border-border bg-card p-6 shadow-sm"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-primary">Channel readiness</p><h2 className="mt-2 text-xl font-semibold">Capability-gated adapters</h2></div><ShieldCheck className="h-6 w-6 text-primary"/></div><div className="mt-6 divide-y divide-border">{(channels.data ?? []).map(channel => <div key={channel.code} className="flex items-center justify-between gap-4 py-4 first:pt-0"><div><p className="font-medium">{channel.displayName}</p><p className="mt-1 text-sm text-muted-foreground">{channel.capability === "api" ? "API path after secure configuration" : channel.capability === "conditional" ? "Prepared until access is confirmed" : "Prepared review workflow"}</p></div><Badge variant="outline" className="capitalize">{channel.operationMode}</Badge></div>)}</div></div><div className="rounded-2xl border border-border bg-card p-6 shadow-sm"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-primary">Activity ledger</p><h2 className="mt-2 text-xl font-semibold">Recorded by design</h2></div><Activity className="h-6 w-6 text-primary"/></div>{data.recentActivity.length ? <div className="mt-6 space-y-3">{data.recentActivity.map(item => <div key={item.id} className="rounded-xl bg-muted/40 p-3"><p className="text-sm font-medium">{item.action}</p><p className="mt-1 text-xs text-muted-foreground">{item.outcome} · {new Date(item.createdAt).toLocaleString()}</p></div>)}</div> : <div className="mt-6 rounded-xl border border-dashed border-border bg-muted/20 p-5"><p className="text-sm font-medium">No activity recorded</p><p className="mt-2 text-sm leading-6 text-muted-foreground">Catalog, inventory, channel, approval, and profile actions will appear here as the workspace is configured.</p></div>}</div></section></div></DashboardLayout>;
}

