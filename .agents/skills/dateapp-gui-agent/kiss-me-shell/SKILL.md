---
name: kiss-me-shell
description: Use when restyling YouAndINotAI authenticated shell to Kiss Me dark mobile GUI.
---
# Kiss Me Shell
- Tokens live in `frontend/react-app/src/index.css` as `--km-*`.
- Shell: `frontend/react-app/src/app/AppShell.tsx` (class `km-shell`).
- Bottom nav: Chats / Message / Create(fab) / Video / Settings.
- Desktop rail includes Create, Animate, Elite, Video lobby.
- Never reintroduce cream brutalist as default app chrome.
- Verify with browser on `/app/create` after `npm run dev` in `frontend/react-app`.
