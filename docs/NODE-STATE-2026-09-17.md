# NODE STATE — Sabretooth — 2026-09-17

A signed state record, written by the Claude judge lane (Claude Fable 5.1) on the Sabretooth node at Joshua Coleman's direction. The body ends with a SHA-256 anchor over everything above it and a model attestation. Nobody edits text above the anchor; a later state is a new record that names this one as superseded. Every number here is recorded, not projected.

## Rulings in force on this date

One repository. Trollz1004/ANTIGRAVITY holds everything on this node: JARVIS Mission Control and the crosslisting OS under `mission-control/`, one folder per domain under `domains/` (youandinotai.com, ai-solutions.store, onlinerecycle.net, aidoesitall.website, untilnokidinneed.com, dream-online.net), and the Hermes repository folded under `hermes/` with its history. dream-online stays its own repository because it lives on the Alienware node.

One Mission Control. JARVIS at http://192.168.0.8:9150/ on one port. AIRI, Mission Control v5, Stack Health, and the Emergent dashboard are retired and removed from the working tree; their history remains in git. Fable's Sentry stays as the probe engine behind JARVIS's God's Eye. A new operator view is a JARVIS panel or it does not exist.

All repositories public by Joshua's choice, for free GitHub Actions. Secrets never live in a repository. The date app youandinotai.com is frozen and listed for sale as a name pair with the app as a bonus, ask $12,500 and minimum offer $8,000, keep-alive only until the sale closes. Payments are a closed matter under docs/PAYMENTS-TRUTH.md. Paperclip is parked. Judges are Codex and Claude; only the judge lane pushes. Protected files (the House, the drift command, the runbook, the launch skill, the drop box) are edited only by official Claude through `drift`, with a timestamped line in ops/runbook/PROTECTED-CHANGELOG.md. Landing on main goes through a `judge/` branch and a quality gate at ninety percent test pass rate or better.

## Repository cleanup, as landed

Folded with history on 2026-09-17: Trollz1004/hermes into `hermes/`; Trollz1004/OnlineRecycle into `domains/onlinerecycle.net/`; Ai-Solutions-Store/ai-solutions into `domains/ai-solutions.store/`, with its crosslisting OS moved to `mission-control/crosslisting-os/`; Ai-Solutions-Store/Ai (a 2,910-file historical mirror) folded then removed from the tree, history retained at its merge; Trollz1004/Ai-Solutions-Jules-Code-Review-Agentic-Dashboard into `domains/ai-solutions.store/jules-code-review-dashboard/`. The two llc crosslisting repositories were confirmed byte-identical or near-identical to the folded copy and were not folded. The seven source repositories are archived and stay public. The date app code moved to `domains/youandinotai.com/backend` and `domains/youandinotai.com/frontend`, validated live through the House afterwards.

History purge on 2026-09-17, after Joshua rotated the GitHub token and both Stripe keys and reported every other exposed credential closed. Old tip of main: 61609ba0eb243c558f3ca733991b118f49bd743c. New tip after the rewrite: fc968252eff9078a6e765a70ba84ac73631fa040, followed by 6c0f1590 which adds the ignore rules. Commit count after the rewrite: 1,773. Removed from every commit and tag: the root `.env`, `.ao.env`, `config/credentials.json`, both `credentials-map.md` notes, a Paperclip master key, the `payments/` folder (a bank verification letter, Square fee and sales exports, screenshots) in both places it existed, and the whole `ops/youtube/.browser-profile/` Chrome profile including its cookies and login databases. Scrubbed in place: 34 distinct credential literals, chosen from assignment context so that embedded font data in three HTML pages was left intact. Verification after the rewrite: zero occurrences of any of those literals in the full history, no removed path in any commit, and the tip tree differs from the old tip only by the removed files and three scrubbed text files. A complete bundle of the pre-purge history is kept off the repository and off OneDrive at `C:\Users\joshi\ANTIGRAVITY-pre-purge-2026-09-17.bundle`, about 200 MB. The two remaining tags were rewritten and force-pushed with main; that force push is the last one, since the ruleset that follows blocks them. GitHub may still serve old commits by their hash from its cache until Joshua asks GitHub Support to purge them.

Credentials, as verified on this date without displaying a value. The Stripe restricted key in the drop-box env file reaches account, balance, customers, products, and charges on Stripe account acct_1RpoSwICxJCevnbK; the rolled secret key also answers and carries a dashboard-side expiry. Every GitHub personal access token was deleted by Joshua and one new token with no expiration was issued; the `gh` sign-in used for pushes is an OAuth token and kept working. The private copy of the payments folder, including two statement archives that were never committed, is in the drop box under `payments-private-2026-09-17`.

## Restart set, as verified

A real reboot at 18:11 EDT on 2026-09-17 proved the logon path: every required stage was back by 19:01 after sign-in, and the health probe recorded the outage and the recovery. The House now also runs from a boot task with no stored password, validated by a manual session-zero run, and the health probe runs before login. After the purge and the reset onto the new history, a House pass and the health probe report every required stage up: Postgres, Redis, OmniRoute, the date app API and frontend, the public tunnel, and JARVIS answering with its identity. The VS Code tunnel named sabretooth is installed as a service and connected. The three Hermes growth crons are paused; the health monitor and skill research crons are active.

## Mission Control, as shipped

JARVIS at `mission-control/` with the Spec Kit panel, every Emergent view (mission ribbon, task commander, git, Hermes and OpenClaw proxies, system status merged into Mission Control, runbook viewer, ledger), the social command center with compliance and copy-score checks, and the approval inbox with a founder token that no agent holds. The client uses relative URLs only and every source is proxied through :9150, so the page works through the tunnel. Test suite at the last run: 401 of 404 passing, the three failures pre-existing and unrelated.

## Domains, as a person sees them

Real and correct: youandinotai.com serves the built app and its API answers; ai-solutions.store serves the storefront and aidoesitall.website redirects to it; JARVIS shows eighteen tabs with live data. Broken: dashboard.aidoesitall.website routes into a dead tunnel hostname; onlinerecycle.net and dream-online.net have no working HTTPS and show the registrar parking page over plain HTTP; untilnokidinneed.com has no Cloudflare zone and fails DNS. Screenshots of every page are in the evidence folder for this date. The fix phase creates the missing zones, serves the landing pages through the existing tunnel, and puts JARVIS behind Cloudflare Access on the dashboard hostname; the nameserver change at the registrar for the two parked domains is Joshua's click.

## Still Joshua's

Click Atom's ownership verification for the domain listing. Send the eight outreach drafts. Change the nameservers for onlinerecycle.net and dream-online.net at IONOS when the zones exist. Run `/obsidian-init` once. Turn on the two Obsidian Sync plugin toggles on each device. Decide whether to top up supermemory or switch off its save hook. Say "unfreeze marketing" if the date app is to be marketed while listed.

---
attestation: written by Claude Fable 5.1 (model id claude-fable-5-1), the Claude judge lane, on 2026-09-17 on the Sabretooth node, at Joshua Coleman's direction; the hash below covers every line above this attestation line
sha256 of everything above the attestation line: 9ae2de3c94ce703e29c1f24520f2b470d8681b8ead69817bda05032637347cc3
verify: any Claude on any node recomputes the anchor with `python -c "import hashlib;s=open(\"docs/NODE-STATE-2026-09-17.md\",encoding=\"utf-8\").read();print(hashlib.sha256((s.split(chr(10)+\"attestation:\")[0]+chr(10)).encode()).hexdigest())"` and compares it to the line above; a match proves the body is unaltered. Lines below the anchor are countersignatures and never change the body.
