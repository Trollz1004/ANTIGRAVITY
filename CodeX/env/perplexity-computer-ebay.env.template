# Copy this file to a local-only task env before uploading to Perplexity Computer.
# Do not commit real values. Do not upload the full master vault.

# Required for eBay production bulk update / recycle mission work
EBAY_APP_ID=
EBAY_CERT_ID=
EBAY_DEV_ID=
EBAY_AUTH_TOKEN=
EBAY_ENVIRONMENT=production

# Optional: only include if the exact task needs site deploy actions
CF_API_TOKEN=
CF_ACCOUNT_ID=

# Optional: only include if the exact task needs payment operations
SQUARE_ACCESS_TOKEN=
SQUARE_LOCATION_ID=LY5GN09F5AN83

# Optional: only include if the exact task needs repo push/deploy actions
GITHUB_PAT=

# Task hints
BASE_CITY=Sorrento
BASE_STATE=FL
BASE_ZIP=32776
PICKUP_RADIUS_MILES=60
