# Skills

Skill library for node 9020 (marketing-node). Layout: one directory per skill, each containing a `SKILL.md` that defines the skill (name, description, instructions).

```
skills/
  <skill-name>/
    SKILL.md
```

No skills are installed yet. This directory is a placeholder until the first skill lands.
