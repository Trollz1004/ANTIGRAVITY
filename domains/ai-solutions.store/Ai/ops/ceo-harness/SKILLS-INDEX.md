# SKILLS-INDEX — pointer only

Two skills loaded, always: `find-skills`, `create-skills`. Nothing else preloaded.

Full shelf (201 skills) not migrated into this repo yet. It still lives at `C:\antigravity\.agents\harness\` / `C:\antigravity\.agents\skills\` on the superseded ANTIGRAVITY repo. Treat that as read-only reference until Josh says it's moved into `Trollz1004/clean`.

Rule: load one skill on demand, use it, drop it. Never preload the shelf — that's what caused drift before.

When the shelf does move here, it belongs at `ops/ceo-harness/skills/` — one file per skill, same on-demand-drop discipline.
