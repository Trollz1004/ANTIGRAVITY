
## 04:39-04:50Z — Freebuff restart recovery + preview registration (Buffy)
- Freebuff restarted overnight: Paperclip (:3100) and CEO bridge (:3140) were down; OmniRoute (:20128), Mission Control (:3151) survived.
- FABLES-HOUSE.cmd (restart launcher) brings up Sabretooth stack ONLY (Postgres/Redis/OmniRoute/frontend/tunnel) — it does NOT start Paperclip or the bridge. Both need manual/detached starts after a restart.
- Paperclip restarted detached: `node.exe <npx-cache>/paperclipai/dist/index.js run` cwd C:\ANTIGRAVITY → :3100, identity verified (ok, 2026.817.0, local_trusted, HEAD ef06ed10). Preview registered on that URL/pid.
- LESSON: bridge must start via `node start.js` (loads gitignored bridge/.env). Direct `node bridge.js` boots but shows agentKeyConfigured:false → CEO heartbeats fail after 1s. start.cmd wraps start.js — equivalent.
- After bridge restart: Buffy CEO runs "Finished" again; failure streak was pre-restart.

## 05:0xZ — Freebuff external adapter built (Buffy)
- Built `ops/paperclip-ceo/adapter-freebuff/` (package.json, index.js, adapter.test.js, README.md): Paperclip external adapter, type `freebuff_local`, exports createServerAdapter() per plugin-loader contract.
- execute(): writes `<runId>.json` wake (kind "adapter") into ops/paperclip-ceo/wakes/, polls for `<runId>.result.json` {status, summary, result}; timeout = honest failure. testEnvironment(): wakes-dir writable + bridge :3140 health (warn-only). getConfigSchema(): wakesDir + timeoutSec.
- CONTRACT for Freebuff session (me): on seeing a wake with kind "adapter", do the work and write the result file. This is how future freebuff_local agent runs complete.
- All 5 verification checks pass (shape, live env test, execute success/timeout/missing-prompt). Install via Paperclip UI → Install External Adapter → Local path → C:\ANTIGRAVITY\ops\paperclip-ceo\adapter-freebuff.
- Uncommitted — judge lane rules before landing.

## 05:53Z — freebuff_local adapter INSTALLED via real plugin-loader (Buffy)
- `paperclipai adapter install --payload-json '{"packageName":"C:/ANTIGRAVITY/ops/paperclip-ceo/adapter-freebuff","isLocalPath":true}'` → 201 {type: freebuff_local, version: 1.0.0, requiresRestart: false}. `adapter list` shows freebuff_local source=external loaded=true. Endpoint: POST /api/adapters/install (instance-admin).
- ROOT CAUSE of initial 500 ("Only URLs with a scheme in: file, data, and node... Received protocol 'c:'"): the running Paperclip process (started 04:38Z) held the OLD 2026.817.0 loader in memory; the npx cache refreshed to 2026.824.0 at 04:56Z (mtime 00:56 local) with the pathToFileURL fix. Proof: disk copy computes serverVersion 2026.824.0, live health said 2026.817.0. Server restart → 824 → install succeeded. LESSON: after an npx cache refresh, restart long-running npx-spawned services or they run stale code.
- Paperclip restarted: PID 13940 on :3100, health 2026.824.0. Preview re-registered.
- npm audit: repo package.json has legacy dep `build ^0.1.4` (2013-era) pulling jxLoader→js-yaml (critical), timespan, uglify-js — 4 of 5 vulns. Nothing in the repo imports 'build'. Removal candidate — needs judge lane like any change.

## 06:0x-06:2xZ — MCPs + skills wired into Paperclip for ALL agents (Buffy)
- Freebuff connectors were NOT down: all 5 proven with real calls (supabase list_tables 34 tables; mission-mcp store_memory id 01M0VRAS; antigravity-files list ops/; brain-mcp enterWorkspace session 950f59a1 opened+closed cleanly — platform ids are sabretooth-node ids like codex-sabretooth, paths must be absolute under C:\ANTIGRAVITY; playwright browser_tabs). UI "disconnected" labels were stale.
- Paperclip tool connections created via Board session (POST /api/companies/92223de0/tools/connections): brain-mcp, mission-mcp, antigravity-files, playwright = local_stdio via stdio-templates; omniroute + supabase = mcp_remote. ALL installed company-wide (PUT installs targetType=company) → buildPaperclipRuntimeMcpServers injects them into every agent run.
- Health checks: 4 stdio = ok. omniroute 502 (needs API-key via credentialSecretRefs — config rejects sensitive keys by design); supabase needs OAuth reconnect. Both are UI clicks, flagged for Joshua.
- Skills: scan-projects mode=import imported 70 new + 15 existing = 87 company skills from .agents/skills.
- Knowledge graph: nodes/9020/vaults/knowledge IS the Obsidian-compatible shared graph (Home.md wiki-links). Added paperclip-capability-baseline note + Home link. Agents read/write via antigravity-files.
- Uncommitted: adapter-freebuff pkg + vault note + package.json (build dep removed, audit 0 vulns). Judge lane next.

## 06:3xZ — ceo-standing-session skill + always-on MCP guarantee (Buffy)
- Created .agents/skills/ceo-standing-session/SKILL.md: session spine (orient → verify → work → prove → judge-lane delivery), MCP usage map, skill routing table, operating law. Imported into Paperclip company skills (scan-projects import) and assigned to CEO agent via POST /agents/:id/skills/sync → desired skill local/3e20d6993c/ceo-standing-session.
- ALWAYS-ON MCP chain completed: tool profile always-on-mcp (f58c43b1, 6 include entries, default deny) → company binding (f5c18f2f) → unscoped gateway always-on-mcp-gateway (cf16ad57, active, applies to every run). KEY FINDING: runtime injection (buildPaperclipRuntimeMcpServers) permits NOTHING without a profile binding — installs alone are inert. Verified via GET /tools/profiles/effective/agents/<CEO>: bindings company-scope, 12 include entries, all 6 connections installed/active.
- Paperclip restarted again after Freebuff restart (PID 7060, 824.0, HEAD 4e1dddd7 — judge lane landed work). Preview re-registered. Bridge :3140 survived.
- Still uncommitted: adapter-freebuff pkg, vault notes, package.json audit fix, this skill. Judge lane next.
