{
  "version": "2.0.0",
  "enforcement": "hard",
  "architecture": "The MD instruction files are the brain, authored and audited by EXTERNAL architects that live OUTSIDE any agent platform (browser-session Claude/Opus per FOUNDER-DOCTRINE rule 6 — the Anthropic hard wall — never an in-platform adapter). Ollama cloud models execute the brain in-platform. Even a 1B model works because the intelligence is in the instruction files, not the weights. Codex AUDITs — it does not do daily operational work.",
  "tiers": {
    "tier_1_workers": [
      "ollama_cloud_primary",
      "ollama_cloud_fallback",
      "pi_ceo"
    ],
    "tier_0_architects": [
      "codex_local"
    ],
    "tier_3_emergency_only": [
      "hermes_ollama_cloud"
    ]
  },
  "adapters": {
    "ollama_cloud_primary": {
      "type": "ollama_cloud",
      "models": [
        "gemma4",
        "glm-5.1:cloud",
        "kimi-k2.6",
        "deepseek-v4-flash"
      ],
      "note": "Any Ollama cloud model works — the MD instruction files contain all the thinking. Covered by Ollama Pro subscription. No per-call API cost.",
      "tier": 1,
      "params": {
        "num_ctx": 131072,
        "temperature": 0.35,
        "top_p": 0.9
      }
    },
    "ollama_cloud_fallback": {
      "type": "ollama_cloud",
      "models": [
        "qwen3.5",
        "gemma4:e2b",
        "nemotron-3-nano"
      ],
      "note": "Smaller/cheaper models. Still works because intelligence is in the prompt files.",
      "tier": 1
    },
    "hermes_ollama_cloud": {
      "type": "ollama_cloud",
      "model": "jeffreyvandekorput/korpohermes-prime:latest",
      "persona": "KorpoHermes Prime — high-agency systems and engineering model for Hermes Agent",
      "params": {
        "min_p": 0.05,
        "num_ctx": 131072,
        "temperature": 0.35,
        "top_p": 0.9
      },
      "tier": 3,
      "note": "Emergency fallback if all Ollama cloud models are unavailable"
    },
    "pi_ceo": {
      "type": "pi_ollama_cloud",
      "model": "glm-5.1:cloud",
      "fallbacks": ["qwen3-coder:480b-cloud", "gemma4"],
      "note": "Pi coding agent via Ollama cloud models. Same intelligence in MD files. Use when Hermes is down or for tasks requiring file-system access.",
      "tier": 1,
      "params": {
        "num_ctx": 131072,
        "temperature": 0.35,
        "top_p": 0.9
      }
    },
    "codex_local": {
      "type": "codex_mcp",
      "role": "ARCHITECT + AUDITOR ONLY",
      "purpose": "Code review, security audit, GitHub workflow maintenance. Does NOT do daily operational work.",
      "tier": 0,
      "when_used": [
        "CI/CD and GitHub Actions maintenance",
        "Security review of code changes",
        "Smart contract audit",
        "Infrastructure changes"
      ]
    }
  },
  "cost_model": {
    "tier_1_ollama_cloud": "Covered by Ollama Pro subscription. No per-call cost. Great usage rates.",
    "tier_0_codex": "Codex audit only. NO in-platform Anthropic adapter (FOUNDER-DOCTRINE rule 6, Anthropic hard wall). Claude/Opus architecture + audit happens via external browser session and is never instantiated or billed in-platform.",
    "savings": "Daily operational work moves from ~$600/mo API calls to $0 incremental (Ollama Pro covers it)"
  },
  "banned": [
    "dateapp-marketingtools",
    "dateapp"
  ],
  "on_banned_adapter_load_attempt": {
    "action": "reject + file URGENT issue + refuse to start heartbeat",
    "who_resolves": "Joshua Coleman only"
  }
}
