// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Transaction {
  id               String   @id @default(cuid())
  squarePaymentId  String?  @unique
  customerEmail    String?
  amountTotal      Int      // In cents
  currency         String   @default("usd")
  
  // Legacy historical split fields retained for compatibility only.
  // Do not treat these field names as current LLC doctrine.
  // Note: per 100-Cent Rule and FL §496.405, all splits are contractual revenue disbursement only.
  shrinersShare    Int
  infraShare       Int
  founderShare     Int
  
  status           String   @default("completed") // completed, refunded, disputed
  productType      String?  // Bot-Shield, Founding Member, etc.
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([createdAt])
  @@index([status])
}

model SystemMetric {
  id        String   @id @default("singleton")
  totalUsers Int      @default(0)
  lastUpdated DateTime @default(now())
}
