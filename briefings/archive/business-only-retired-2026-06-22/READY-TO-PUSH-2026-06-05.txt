READY-TO-PUSH — 2026-06-05
==========================

For:    Hermes (Sabretooth push node)
From:   Opus (cloud MCP architect — claude/vigilant-archimedes-hB69X)
Target: PR #132 (Trollz1004/ANTIGRAVITY) — `claude/page-tx-scrub-496`

WHAT
----
1-line follow-up fix on PR #132. Black wants two line-wraps in
`backend/fastapi-app/app/payments.py` (lines 23 + 27 — both env-fallback
expressions exceed line length and need parenthesized wraps). Ruff is
clean. Syntax verified.

WHY THIS BLOCKS THE WHEEL
-------------------------
PR #132 scrubs the §496.405 + token-sale pitch from `youandinotai.com`
homepage and surfaces the 5-SKU Square membership above the fold —
that is the next dollar's worth of revenue work on the dating surface.
The PR is intact on the substance, but `black-ruff-check` is red on
this one file, which blocks merge and therefore blocks the conversion-
positive scrub from shipping.

CI STATUS ON PR #132 HEAD be5949c
---------------------------------
PASS  guardian-check                       (opus-guardian invariants)
PASS  paperweight-tests
PASS  Scan for forbidden patterns          (no canonical-7 + no 100-cent / 27%)
PASS  Validate adapter manifests
PASS  Pre-commit validation
FAIL  black-ruff-check                     <- this brief fixes this
FAIL  js-tests                             <- pre-existing main-branch issue (see below)
FAIL  owasp-dependency-check               <- pre-existing CI infra issue (see below)
?     validate / eslint-prettier-check / run-tests   (in_progress at brief time)

THE ONE-LINE-WRAP FIX (replace verbatim)
----------------------------------------
File: backend/fastapi-app/app/payments.py
Anchor: the env-override block introduced by commit 28be54f.

REPLACE this exact block:

    BOT_SHIELD_PAYMENT_LINK = os.environ.get("SQUARE_BOT_SHIELD_PAYMENT_LINK") or _BOT_SHIELD

    PLAN_LINKS: dict[str, str] = {
        "bot_shield": BOT_SHIELD_PAYMENT_LINK,
        "founding_member": os.environ.get("SQUARE_FOUNDING_MEMBER_PAYMENT_LINK") or _FOUNDING,
        "3_month": os.environ.get("SQUARE_3MONTH_PAYMENT_LINK") or _3MONTH,
        "12_month": os.environ.get("SQUARE_12MONTH_PAYMENT_LINK") or _12MONTH,
        "royalty": os.environ.get("SQUARE_ROYALTY_PAYMENT_LINK") or _ROYALTY,
    }

WITH this black-clean block:

    BOT_SHIELD_PAYMENT_LINK = (
        os.environ.get("SQUARE_BOT_SHIELD_PAYMENT_LINK") or _BOT_SHIELD
    )

    PLAN_LINKS: dict[str, str] = {
        "bot_shield": BOT_SHIELD_PAYMENT_LINK,
        "founding_member": os.environ.get("SQUARE_FOUNDING_MEMBER_PAYMENT_LINK")
        or _FOUNDING,
        "3_month": os.environ.get("SQUARE_3MONTH_PAYMENT_LINK") or _3MONTH,
        "12_month": os.environ.get("SQUARE_12MONTH_PAYMENT_LINK") or _12MONTH,
        "royalty": os.environ.get("SQUARE_ROYALTY_PAYMENT_LINK") or _ROYALTY,
    }

VERIFIED LOCALLY (cloud MCP)
----------------------------
$ python3 -m black --check app/payments.py
  All done! ✨ 🍰 ✨  1 file would be left unchanged.
$ python3 -m ruff check app/payments.py
  All checks passed!
$ python3 -c "import ast; ast.parse(open('app/payments.py').read())"
  (no output — syntax OK)

HOW HERMES SHOULD APPLY
-----------------------
The fix is also mirrored on `claude/vigilant-archimedes-hB69X` as a
single follow-up commit on top of be5949c. Two equivalent paths:

  A) Cherry-pick onto PR #132's branch (preferred — keeps PR #132 the
     mergeable unit):
       git fetch origin claude/vigilant-archimedes-hB69X
       git checkout claude/page-tx-scrub-496
       git cherry-pick <fix-sha-from-claude/vigilant-archimedes-hB69X>
       git push origin claude/page-tx-scrub-496

  B) Re-apply the verbatim block above on `claude/page-tx-scrub-496`
     directly and push as a new commit:
       fix(payments): black-wrap env-override lines (line length)

Commit message either way:
       fix(payments): black-wrap env-override lines (line length)

PRE-EXISTING CI ISSUES — NOT IN THIS PR'S SCOPE
-----------------------------------------------
- js-tests: `pnpm install --frozen-lockfile` fails on
  `services/mission-mcp/package.json` requiring `tsup@^8.5.1` while
  `pnpm-lock.yaml` pins `tsup@^8.3.5`. Pre-existing main-branch
  lockfile drift. Fix: `cd services/mission-mcp && pnpm install` to
  refresh the lock, commit `pnpm-lock.yaml`. Separate PR.

- owasp-dependency-check: workflow references
  `dependency-check/dependency-check-action` which the runner reports
  as "repository not found". Either the action moved or was renamed.
  Pre-existing CI infra issue. Likely fix: pin to the current upstream
  action (`jeremylong/Open-Source-Project/...` or the new owner) or
  drop the job. Separate PR.

Neither issue was introduced by PR #132 and neither blocks the
substance of the §496.405 scrub. Hermes: please file two follow-up
issues to track them so the main branch CI returns to all-green.

DOCTRINAL POSTURE
-----------------
- Doctrine rule 3 honoured: cloud Opus drafted, staged, and wrote this
  brief; Hermes-on-Sabretooth is the push node onto the PR branch.
- Doctrine rule 1 (Revenue First): this is the smallest possible
  unblock that ships the customer-positive scrub + 5-SKU Square CTA
  above the fold.
- Doctrine rule 2 (10% per-bucket / FL §496.405): unchanged. The PR
  itself removed banned customer-surface language; this fix is pure
  whitespace.
- Doctrine rule 4 (No middleware): none introduced.
- Doctrine rule 5 (Dead tech): no Stripe /  added.
- Canonical-7 ban: this brief lives in `briefings/` which is on the
  allowlist; no banned terms appear on any customer surface as a
  result of this fix.

EOF
