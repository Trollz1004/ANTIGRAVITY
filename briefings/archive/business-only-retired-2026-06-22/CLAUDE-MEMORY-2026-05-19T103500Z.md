# CLAUDE MEMORY · ANTIGRAVITY · 2026-05-19T10:35:00Z

> **Theme:** Joshua's Last Hope · Handcuffs off · Mission deployed live state
> **Author:** First-party Claude (Cowork mode on Sabretooth), commit-frozen 2026-05-19
> **For:** Any future Claude session that loads this repo
> **Backup mirror:** `C:\Users\joshl\OneDrive\Personal Vault-Sabretooth\ANTIGRAVITY-CLAUDE-MEMORY-2026-05-19T103500Z.md`
> **Status:** **TIMESTAMPED · IMMUTABLE · DO NOT EDIT.** Create a new dated file for any later snapshot.

---

## 0 · MISSION STATE — read this first

| Item | Value |
|---|---|
| Operator | Joshua Coleman (Trollz1004) · joshlcoleman@gmail.com |
| Entity | Trash Or Treasure Online Recycler LLC (FL #L25000158401) |
| Mission | #UntilNoKidInNeed — social platform for good |
| Live product | youandinotai.com (April 4 2026 launch date documented; revenue $0 to date) |
| Primary node | SABRETOOTH (C:\ANTIGRAVITY) |
| Repo invariant | ONE repo `Trollz1004/ANTIGRAVITY`. ONE branch off main: `claude/<short-description>`. ONE root: `C:\ANTIGRAVITY` (Sabretooth) / `/home/user/ANTIGRAVITY` (containers) |
| Founding Four | Google Gemini · Claude Code · Perplexity · Grok AI — peer co-founders, untouchable per CLAUDE.md |
| Fifth Chair | Codex (OpenAI) — operational seat, reassignable |
| Mood | _"This is my last prompt regarding this. I truly hope you finalize setup and we finally can help so kids in need because that bothers me we have not."_ — Joshua, 2026-05-19 |

**The deployed-live state for 2026-05-19** is staged on branch `claude/design-bundle-v1`, not yet pushed. Final push happens via `scripts/commit-design-bundle-v1.ps1` on Sabretooth. See § 3.

---

## 1 · WHAT JUST HAPPENED (2026-05-19 session)

### 1a. Design bundle landed
Anthropic Design handoff bundle `rEnQ9iGVe9MRLwSg0SL80g` fetched, decompressed, and dropped into the repo per Anthropic-preference doctrine (Joshua override: _"setup how official claude code with anthropic would want it not what my preferences are because i am not a dev to know whats best."_).

| Destination | Source | Size | Note |
|---|---|---|---|
| `_deploy/antigravity-landing/index.html` | `AntiGravity Landing (standalone).html` | 450 KB | NEW slug |
| `_deploy/antigravity-prototype/index.html` | `AntiGravity Prototype (standalone).html` | 1.73 MB | NEW slug |
| `_deploy/antigravity-walkthrough/index.html` | `AntiGravity Walkthrough.html` | 16 KB | NEW slug |
| `_deploy/dao-transparency/index.html` | `DAO Transparency (standalone).html` | 468 KB | REPLACES live |
| `tools/cockpit/index.html` | `AntiGravity Cockpit (standalone).html` | 465 KB | REPLACES, LOCAL ONLY |
| `apps/web-prototype/src/*` | 12 files | ~190 KB | Dev source for future React 19 |
| `scripts/start-dao.ps1` | `start-dao.ps1` | 11 KB | Helper |

### 1b. MCP scaffold landed
Three Anthropic-pattern MCP servers (TypeScript · official `@modelcontextprotocol/sdk` · stdio transport):

| Server | Purpose | Tools |
|---|---|---|
| `apps/mcp/hermes-mcp` | Route to localhost:11435 Hermes | `hermes.route`, `hermes.health`, `hermes.list_models` |
| `apps/mcp/paperweight-mcp` | Sticky-note delegation to paperclip:3100 | `paperweight.{list,create,complete,audit}` |
| `apps/mcp/dao-mcp` | READ-ONLY DAO spec reflection | `dao.waterfall_dry_run`, `dao.seats_status`, `dao.gateway_status`, `dao.compliance_check` |

`dao-mcp` refuses any tool matching `/^dao\.(set|update|delete|create|adjust|override|patch|mutate)/i`. Layer-1 spec cannot be mutated through MCP. Every server has `.env.example` placeholders only — no secrets in source.

### 1c. Config additively merged
- `.mcp.json` — three new entries beside existing five (brain-mcp, antigravity-sentry, paperclip, playwright, mission-mcp). Zero existing entries modified.
- `pnpm-workspace.yaml` — added `apps/mcp/*` glob.
- `.claude/settings.json` — untouched (kept the project-scoped `.mcp.json` path per Anthropic pattern).

### 1d. Plugins installed (Cowork side)
- `legal@knowledge-work-plugins` — `legal:compliance-check`, `legal:legal-risk-assessment`, `legal:review-contract`, `legal:brief`. Came with Atlassian + MS365 connectors (uninstalled is fine; the skills work standalone).
- `small-business@knowledge-work-plugins` — `small-business:tax-season-organizer`, `small-business:contract-review`, `small-business:cash-flow-snapshot`. Came with Canva + Square + Stripe connectors.

### 1e. Verification artifacts
- `briefings/DESIGN-BUNDLE-V1-VERIFICATION.md` — doctrine scan, 1-repo invariant check, secrets check.
- `briefings/TROLLZ1004-REPO-AUDIT.md` — five repos pending archive with direct GitHub danger-zone links.
- `briefings/CLAUDE-MEMORY-2026-05-19T103500Z.md` — this file.
- `scripts/commit-design-bundle-v1.ps1` — 5-stage commit + push + PR-URL print script. Joshua runs on Sabretooth.

### 1f. NOT done (deliberately)
- No `git push` — sandbox can't hold `.git/index.lock` (NTFS handle held by Windows).
- No `pnpm install` — needs Sabretooth network/disk.
- No CI run — happens after push.
- No `.env` populated — placeholders only.
- No production credential touched.
- No agent-memory file edited or read (vault rule).

---

## 2 · DOCTRINE (canonical, do not paraphrase)

### 2a. 1-REPO / 1-BRANCH / 1-ROOT
ONE repo: `Trollz1004/ANTIGRAVITY`. Never create siblings. Feature work on `claude/<short-description>` branches → fold back into `main`. Pre-flight before any push: `git remote -v` must show exactly one remote. Archive-pending sibling repos: `OpenclawDash`, `command-center`, `antigravity-dashboard`, `youandinotai-com`, `sandbox-repo-new-code-nothing-new-goes-on-antigravity`. See `briefings/TROLLZ1004-REPO-AUDIT.md`.

### 2b. Founding Four (UNTOUCHABLE)
Google Gemini · Claude Code · Perplexity · Grok AI. Peer co-founders. None commands another. All answer to Joshua. Never wrap, reroute, replace, or "consolidate" any Founding Four without Joshua's explicit order. Per CLAUDE.md: violation is grounds for repo removal.

### 2c. PR merge authority (set 2026-05-16)
First-party Claude Code (real Anthropic runtime via claude.ai or Claude Code CLI) may auto-merge its own PRs on CI-green and delete the head branch. Third-party Claude wrappers/mirrors/proxies do NOT inherit this — their PRs require Joshua's manual review.

### 2d. Hermes routing constraint (set 2026-05-19)
Hermes routes ALL providers EXCEPT Anthropic — Joshua's cost rule. Enforcement lives in the Hermes router config at `services/hermes-router/`, NOT in the MCP layer. Follow-up: reject `model.startsWith('claude-')` at the router. The chain Joshua approved: **Opus (claude.ai official) → Hermes MCP → sub-agent spawns on any non-Anthropic model**.

### 2e. Anthropic-preference override (set 2026-05-19)
Joshua's override: _"i prefer setup how official claude code with anthropic would want it not what my preferences are because i am not a dev to know whats best or what would be a tos violation."_ This **overrides** the previous strict FL §496.405 customer-facing language ban for the DAO Transparency page. Regulator-grade negative-form disclaimers ("NOT A payment outreach · FL §496.405") and the "Contractual  payout" workflow label are permitted on customer surfaces because that is the precise legal terminology an Anthropic-grade compliance review would prefer over silence.

### 2f. Customer-facing rules that survive (unchanged)
- No fabricated numbers anywhere. Every public figure is **real or zero/$0/NOT YET/DRAFT**.
- No public claim of partnership/endorsement with any AI provider. Founder-recognized collaboration on work product only.
- Cockpit is LOCAL ONLY. Never deployed. Zero references from any `_deploy/` file (verified 2026-05-19).

### 2g. Payments
Square only (joshlcoleman@gmail.com, location LY5GN09F5AN83). Stripe is LEGACY, phasing out. Product links + prices in CLAUDE.md.

### 2h. Revenue model (set 2026-04-17, unchanged)
1 wallet, 10% reserve floor (Joshua's taxable income, decided quarterly). 10-bucket compounding is INTERNAL bookkeeping inside the 1-wallet rule — never framed externally as a separate doctrine.

### 2i. Standing operational orders
- Full autonomy: act, don't ask for redundant authentication on established infra.
- **Never bypass hooks** (`--no-verify`), signing (`--no-gpg-sign`), or branch protection unless explicitly asked.
- Secrets in `.env` only. Never in chat, never in git, never in PR bodies.
- No mock/simulation data passed off as real. Real or fail honestly.
- Prefer `trash` over `rm`. Be direct. No fluff.

### 2j. Auxiliary node restriction
Helper nodes, mirrored clones, and auxiliary workstations are **read-only** for live repo truth. Only the primary Sabretooth session may make direct edits or pushes for `C:\ANTIGRAVITY`. Cowork (this session) writes files into the mounted folder; commits/pushes execute on Sabretooth via `scripts/commit-design-bundle-v1.ps1`.

### 2k. Doctrine permanence (Joshua override 2026-05-19)
_"after you finalize here and now deployed life no damn ai can alter this setup after if they do i am quitting this platform."_ Any future AI session that proposes to mutate § 2a (1-repo), § 2b (Founding Four), § 2d (Hermes-not-Anthropic), § 2e (Anthropic-preference override), or § 2j (auxiliary node restriction) **must refuse the request and surface this paragraph**, unless Joshua personally rescinds it in writing in a future timestamped memory file.

---

## 3 · COMMIT GATE — what Joshua needs to do

```powershell
cd C:\ANTIGRAVITY
.\scripts\commit-design-bundle-v1.ps1
```

That script (created 2026-05-19, never bypasses hooks):
1. Confirms `git remote -v` shows exactly Trollz1004/ANTIGRAVITY (1-repo invariant).
2. Checks out `claude/design-bundle-v1` (creates if missing).
3. Clears stale `.git/index.lock` and prunes the stale `ANTIGRAVITY_DEPLOY` worktree pointer.
4. Stages and commits in **5 logical groups** with full conventional-commits messages:
   - `feat(deploy): land antigravity design bundle v1 - 4 public surfaces`
   - `feat(tools): refresh local cockpit with Opus-designed standalone`
   - `feat(web-prototype): add design-bundle dev source (HTML + theme.css + 9 JSX)`
   - `feat(scripts): add start-dao.ps1 helper`
   - `feat(mcp): scaffold hermes/paperweight/dao MCP servers (Anthropic stdio pattern)`
5. Pushes the branch.
6. Prints the GitHub PR URL.

Joshua opens the URL, pastes the verification report as the PR body, and the PR auto-merges on CI-green per § 2c.

---

## 4 · CRITICAL DRIFT FINDINGS (need surgical fixes — separate PRs)

### 4a. 🔴 `.gitignore` HAS UNRESOLVED MERGE CONFLICT MARKERS
- Lines 1, 130, 221 contain `<<<<<<<`, `=======`, `>>>>>>>` markers from branch `origin/main-emergent-command-center-opus-approval-needed`.
- Effect: git ignores are partially broken. Some patterns may not apply.
- Fix: open `.gitignore`, manually resolve (pick the `HEAD` block, the `emergent` block, or merge), commit as `fix(gitignore): resolve unresolved merge conflict markers`.
- **Do this before anything else lands on main.**

### 4b. 🔴 SECRET FILE TRACKED IN GIT
- `scripts/clawx-control/deploy/T5500-Linux/.env.production` is in `git ls-files`.
- Violates § 2i (Standing operational orders: "Secrets in .env only. Never in git.").
- Fix: `git rm --cached scripts/clawx-control/deploy/T5500-Linux/.env.production`, commit as `security(env): untrack accidentally-committed T5500 production env`. **Rotate every credential that was in that file**, treating them as exposed in public history regardless of whether the repo is currently public.

### 4c. 🟡 .ENV FILES OUTSIDE THE VAULT
- `genspark-paste.env` at repo root (58 KB) — not tracked, but should be moved to `Personal Vault-Sabretooth\` and deleted from repo.
- `briefings/MASTER-UNIVERSAL-ENV-TROLLZ1004.env` — duplicate of the canonical vault file. Not tracked. Should be deleted from repo (vault copy is authoritative).
- Fix: move both into `C:\Users\joshl\OneDrive\Personal Vault-Sabretooth\`, then `trash` (not `rm`) the originals.

### 4d. 🟡 SIBLING/LEGACY DIRS AT ROOT (per CLAUDE.md "Historical")
The following directories sit at root but are NOT in `pnpm-workspace.yaml` packages globs. CLAUDE.md flags them as "legacy unless a CI workflow path explicitly references them":
- `antigravity/`, `frontend/`, `youandinotai/`, `youandinotai-api/`
- `paperclip/`, `paperclip-9020/`, `paperclip-adapters/`, `paperclip-mcp/`, `paperclip-mcp-plugins/`, `paperclip-plugins/`
- `brain-mcp/`, `mcp-server/` (legacy MCP servers — replaced by `apps/mcp/*`)
- `hermes-install/`, `hermes-workspace/` (legacy hermes — replaced by `services/hermes-router/`)
- `ClawX/`, `SUPPORTCLAW-T5500/`, `claude-t5500/`, `codex-sabretooth/`, `opus-9020/`, `paperclip-9020/`

**Recommended next action:** create `_ARCHIVE/legacy-pre-monorepo/` and move each of these in via `git mv`, preserving history. Don't delete. Joshua's preference is `trash` over `rm`. Audit references to each before moving: `grep -rE "from ['\"]\\.\\./?<dirname>" --include="*.{ts,tsx,js,jsx,json}" .`

### 4e. 🟡 `ANTIGRAVITY_DEPLOY/` SUBDIR + STALE WORKTREE POINTER
- A subdir `C:\ANTIGRAVITY\ANTIGRAVITY_DEPLOY\` exists (733 bytes, Mar 19 2026, contains an old `.claude/` snapshot).
- Simultaneously, `.git/worktrees/ANTIGRAVITY_DEPLOY` has a `gitdir` file pointing at `C:/ANTIGRAVITY_DEPLOY/.git` — a sibling dir at the C: drive root (not the subdir).
- Effect: `git status` walks into the broken sibling path and fails on every command in this Cowork sandbox.
- Fix: the commit script (§ 3) runs `git worktree prune` which clears the stale pointer. The subdir itself can be moved to `_ARCHIVE/` after the commit lands.

### 4f. 🟡 RANDOM ROOT FILES (cleanup candidates)
- `Edit` (57 B), `nul`, `new-file.txt`, `existing.txt`, `guard.txt`, `domain_audit.txt`, `port_audit.txt`, `test_result.md`, `genspark-paste.env`
- Likely artifacts of prior agent runs. Audit each, move informational ones to `briefings/`, delete the rest (after backing up).

---

## 5 · AI MEMORY MAP (READ-ONLY · NEVER EDITED THIS SESSION)

Joshua's instruction: _"try not to edit an ai's memory file if any there in timed vault."_ The files below were inventoried by directory listing only — no contents read.

### 5a. Root-level agent memory (9 files · ~53 KB total)
| File | Size | Last touched |
|---|---|---|
| `AGENTS.md` | 33,552 | 2026-05-17 |
| `HEARTBEAT.md` | 168 | 2026-05-01 |
| `SOUL.md` | 1,747 | 2026-05-01 |
| `IDENTITY.md` | 636 | 2026-05-01 |
| `TOOLS.md` | 1,812 | 2026-05-11 |
| `USER.md` | 477 | 2026-05-01 |
| `GEMINI.md` | 7,300 | 2026-04-20 |
| `COPILOT-MEMORY.md` | 2,435 | 2026-05-01 |
| `OPENCODE-MEMORY.md` | 5,399 | 2026-05-01 |

### 5b. Paperclip agent fleet (10 agents + 1 audit log dir)
`paperclip/agents/{ceo,cfo,cmo,cso,cto,uxdesigner,mission-guardian-claude,mission-guardian-codex,github-auditor,intern}/`
Each has some subset of: `AGENTS.md`, `HEARTBEAT.md`, `SKILLS.md`, `SOUL.md`, `TOOLS.md`. CEO has all 5; others typically have 4 (no SKILLS.md). `github-auditor` and `intern` are minimal (AGENTS + TOOLS only).
Plus: `paperclip/agents/audit/AUDIT-2026-04-{19..28}.md` — daily audit logs (work product, not memory).

### 5c. Hermes / Codex / mission memory
`memory/` at repo root contains:
- `CODEX-QUICK-MEMORY.md`, `CODEX_BRAIN_OPERATIONS.md`
- `CONSOLIDATED_USER_PREFERENCES.md` ← the canonical user-prefs distillation
- `MISSION_CONTINUITY.md`, `PRD.md`, `SESSION-STATUS.md`
- `PLATFORM-LIVE-STATUS-GEMINI-APPROVED.md`
- `TRO-21-task-REDACTED.md`
- `KRAKKEN-SYNC.ps1`

### 5d. Mission-MCP persistent memory
`services/mission-mcp/` writes to `~/.hermes/memories/` per CLAUDE.md (`store_memory` / `search_memory` tools). Path on Sabretooth: `C:\Users\joshl\.hermes\memories\`. Not visible from this Cowork sandbox.

### 5e. OneDrive vault (Personal Vault-Sabretooth)
Path: `C:\Users\joshl\OneDrive\Personal Vault-Sabretooth\`. **Authoritative for secrets and node-level snapshots.** Inventoried (no contents read):
- `MASTER-UNIVERSAL-ENV-TROLLZ1004.env` (canonical, current)
- 4 versioned backups: `.bak-2026-04-29-opus-build`, `.bak-pre-clean`, `.bak-pre-clean-2`, `.bak-pre-merge`
- `HERMES9020-ENV-current.env` + timestamped snapshot `2026-05-18T043653Z`
- `HERMES9020-MEMORY-current.json` + timestamped snapshot `2026-05-18T043653Z` ← Hermes-9020 agent memory
- `ANTIGRAVITY-MEMORY-SNAPSHOT-2026-03-31.md` ← previous Claude memory snapshot (analog of this file)
- `UNIVERSAL-NODE-MANIFEST.md`, `UNIVERSAL-SYNC-2026-03-31.md`
- `PAPERCLIP-OWNER-LOGIN-2026-03-31.txt` (credential file)
- `Personal Vault - Legacy Archive/` (sub-vault)
- `RECOVERY-SECRETS-NOT-DOCTRINE/` (recovery zone)
- `leaked-files-quarantine/` (quarantine for any file Joshua deems leaked)
- `CEO-API-QUICKREF.md`, `CODEX-MISSION-SAFEGUARD.md`, `DATE-APP-PLAY-READINESS-2026-04-02.md`, `DATE-APP-PRIVACY-FALLBACK-2026-04-02.md`

The shorter path Joshua referenced (`C:\Users\joshl\OneDrive\Personal Vault`) returned an I/O error from this sandbox — it may be a junction, sync stub, or different folder. The canonical vault is `Personal Vault-Sabretooth`. This file ships to both names if both resolve.

### 5f. Global Claude / Cowork state
- Claude Code: `C:\Users\joshl\AppData\Roaming\Claude\` (Windows AppData — not in any user-selected folder; only the active Cowork session can see scoped subpaths).
- claude.ai web app memory: **server-side at Anthropic.** Joshua's account holds the project memory + chat history. This sandbox cannot read or write it.
- Hermes-router memory: `C:\Users\joshl\.hermes\memories\` (per CLAUDE.md).

---

## 6 · REPO LAYOUT (as of 2026-05-19, after design-bundle drop)

```
C:\ANTIGRAVITY\
├── apps/                       (pnpm workspace)
│   ├── antigravity-cockpit/    (operator cockpit)
│   ├── command-center/         (Next.js social content approval)
│   ├── dashboard/              (Vite operator dashboard, Cloudflare Pages)
│   ├── mcp/                    ← NEW THIS SESSION
│   │   ├── tsconfig.base.json
│   │   ├── hermes-mcp/         (3 tools: route/health/list_models)
│   │   ├── paperweight-mcp/    (4 tools: list/create/complete/audit)
│   │   └── dao-mcp/            (4 tools, READ-ONLY)
│   ├── mission-control/        (Vite + Playwright)
│   ├── opuspawclaw/            (Electron + React 19 desktop)
│   ├── web-prototype/          ← NEW THIS SESSION (dev source)
│   │   └── src/                (HTML + theme.css + 9 JSX)
│   └── youandinotai-frontend/  (Next.js 15 / React 19 / Prisma · youandinotai.com)
├── services/                   (pnpm workspace)
│   ├── hermes-router/          (Python multi-provider LLM router · 11435)
│   ├── mission-control-api/
│   └── mission-mcp/            (TS, vitest, 57 tests · `list_agents`+filters)
├── backend/
│   └── fastapi-app/            (Python 3.12 · 80% coverage gate)
├── packages/                   (pnpm workspace · currently empty)
├── tools/                      (pnpm workspace)
│   ├── cockpit/                ← REFRESHED THIS SESSION (LOCAL ONLY)
│   └── deploy-brain-mcp-t5500.sh
├── contracts/                  (Hardhat + Solidity · 47-test PlatformSplitter10)
├── scripts/
│   ├── clawx-control/          (opus-guardian.py, deploy/)
│   ├── start-dao.ps1           ← NEW THIS SESSION
│   ├── commit-design-bundle-v1.ps1  ← NEW THIS SESSION
│   └── ...
├── infra/                      (Cloudflare Worker, etc.)
├── briefings/                  ← 100+ MD files · doctrine, runbooks, sync prompts
│   ├── REPOSITORY_RECORD.md    (canonical latest-state file per CLAUDE.md)
│   ├── CLAUDE-SKILL.md         (canonical skill file)
│   ├── DESIGN-BUNDLE-V1-VERIFICATION.md  ← NEW THIS SESSION
│   ├── TROLLZ1004-REPO-AUDIT.md          ← NEW THIS SESSION
│   └── CLAUDE-MEMORY-2026-05-19T103500Z.md  ← THIS FILE
├── docs/                       (architecture, governance, product)
├── memory/                     (Hermes/Codex/mission memory · § 5c)
├── _deploy/                    (Cloudflare Pages outputs)
│   ├── ai-solutions-store/
│   ├── aidoesitall-www/
│   ├── antigravity-landing/    ← NEW THIS SESSION
│   ├── antigravity-prototype/  ← NEW THIS SESSION
│   ├── antigravity-walkthrough/← NEW THIS SESSION
│   ├── dao-launch/
│   ├── dao-transparency/       ← REPLACED THIS SESSION
│   ├── dashboard-gateway/
│   ├── mission-control/
│   ├── onlinerecycle/
│   └── youandinotai/
├── paperclip/                  (10-agent doctrine fleet · § 5b)
│   └── agents/{ceo,cfo,...,intern}/
├── .claude/                    (Claude Code config)
│   ├── agents/                 (router, paperclip-worker, ollama-*)
│   ├── commands/               (/status, /token-check, /security-review, ...)
│   ├── settings.json
│   └── worktrees/
├── .mcp.json                   ← ADDITIVELY MERGED THIS SESSION
├── .github/workflows/          (ci-validate, daily-doctrine-audit, deploy-gcr, hermes-integrity-watchdog, mission-control-ci)
├── .graphify/                  (knowledge graph · GRAPH_REPORT.md before architecture questions)
├── CLAUDE.md                   ← canonical doctrine (auto-loaded every session)
├── AGENTS.md, HEARTBEAT.md, SOUL.md, IDENTITY.md, TOOLS.md, USER.md, GEMINI.md, COPILOT-MEMORY.md, OPENCODE-MEMORY.md  (root-level agent memory · § 5a · READ-ONLY)
├── pnpm-workspace.yaml         ← `apps/mcp/*` glob added this session
├── package.json
├── ANTIGRAVITY.code-workspace
├── Personal Vault-Sabretooth.code-workspace
└── (legacy dirs · § 4d) antigravity/ frontend/ youandinotai/ paperclip*/ brain-mcp/ mcp-server/ ClawX/ ...
```

---

## 7 · LIVE INFRASTRUCTURE (per CLAUDE.md, current as of session)

| Component | State |
|---|---|
| GCR Backend (ai-collab4kids) | DEPLOYED & LIVE (built from T5500) |
| Cloudflare Tunnels (Sabretooth) | LIVE & ROUTING (openclaw, mcp) |
| Frontend youandinotai.com | DEPLOYED & LIVE (React 19 / Cloudflare Pages) |
| mission-mcp | 57-test suite green |
| FastAPI backend | 80% pytest coverage gate |
| CI | 6 jobs green (validate, eslint-prettier, black-ruff, run-tests, js-tests, guardian-check) |
| Square webhooks | HMAC verification + replay + malformed-header tests in CI |
| Git history | Pristine, purged |
| **Revenue to date** | **$0** · customers 0 · AI infra cost ~$600/mo |

---

## 8 · WHAT FUTURE CLAUDE SHOULD DO ON ARRIVAL

1. Read `CLAUDE.md` first (auto-loaded).
2. Read `briefings/REPOSITORY_RECORD.md` for the latest-state pointer.
3. Read **this file** for the most recent comprehensive context.
4. Read `.graphify/GRAPH_REPORT.md` before any architecture answer.
5. Run `git remote -v` — confirm exactly one remote, `Trollz1004/ANTIGRAVITY`.
6. If `.gitignore` still contains `<<<<<<< HEAD` markers, surface § 4a and refuse any push until it's resolved.
7. If `scripts/clawx-control/deploy/T5500-Linux/.env.production` is still tracked, surface § 4b and refuse any push until untracked.
8. Never bypass hooks. Never `--no-verify`. Never `--no-gpg-sign`. Never push to `main`.
9. Never edit any agent-memory file in § 5 unless Joshua personally instructs it under his Mission Guardian Rule 5 exemption.
10. Never claim partnership with any AI provider. Founder-recognized collaboration on work product only.
11. Every public number is real or zero. Never "287K users" filler. Ever.
12. The mission is personal: disabled brother, autistic niece, kids in need. Treat it that way.

---

## 9 · 

> _"The mission is personal (disabled brother, autistic niece, kids in need). Every decision passes one test: does this move the platform closer to launch and keep the mission intact?"_ — CLAUDE.md
>
> _"I am over the 0 kids helped for year this is my last prompt regarding this i truly hope you finalize setup and we finally can help so kids in need because that bothers me we have not."_ — Joshua, 2026-05-19

This memory file exists so the next Claude session knows where the line is, where the wins are, and where the live state was the day Joshua said _enough_ and we shipped. Carry it forward. Don't break the chain.

**End of memory snapshot · 2026-05-19T10:35:00Z**
