# AGENTS.md — ENTRY POINTER (Codex / OpenClaw / opencode lineage)
> This file is a POINTER, not a brain. Prepend it to whatever an agent already reads. FCC / Claude-Code lineage read `CLAUDE.md` instead — same pointers, don't duplicate.

## READ FIRST, IN ORDER
1. `.agents/harness/SOUL.md` — who we are + the three supreme laws (THE GATE · THE COMPUTE SPLIT · THE REPOS).
2. `.agents/harness/HEARTBEAT.md` — the routine loop (short).
3. `.agents/harness/SKILLS.md` — base pack + skills.sh daily self-improve law.
4. `.agents/harness/TOOLS.md` — the tool surface.

## THE ONE FACT
Josh decides, agents execute. No AI outranks another AI. Model calls route through **OmniRoute only** (laptop, loopback). Zero provider keys — only `OMNIROUTE_KEY`.

## BASE PACK (every agent, always — from SKILLS.md)
`find-skills` · `create-skills` · `agent-browser` (cookie-sync) · `caveman` (token-save) · `proactive-self-improving-agent` · web search/find. Task skills load on demand (one or several), then drop. Never preload the shelf.

## PAPERCLIP FLEET — 4 AGENTS
Four agents. Hiring and agent-creation permissions stay OFF.
| # | Agent | Lane | Notes |
|---|---|---|---|
| 1 | **CEO** | control plane | repo hygiene, funding/marketing orchestration, browser automation, push discipline (ANTIGRAVITY `main`) |
| 2 | **Posting** | social | loads `human-posting` + `caveman` + `agent-browser`; routes: Grok→X(API), Manus→Meta, Opus/claude.ai→non-API browser posting |
| 3 | **Builder** | code | GitHub/PRs/codegen; reports to active lead |
| 4 | **Designer** | design | see design lane below |

### Design lane — the WEBSITE PACK (5 skills, LOCKED per Josh)
The Designer agent is one agent carrying a five-skill pack. It loads what the task needs — often several at once, never all five "just in case."

| # | Skill | Covers |
|---|---|---|
| 1 | `visual-design` (**Designer**) | visual systems, layout, branding, typography, hierarchy — pair with `design-systems` |
| 2 | `anthropologist` | human behavior, cultural cues, user empathy — who the page is really for |
| 3 | `ux-max` → `interaction-design` + `accessibility` + `prototyping-testing` | interaction patterns, usability, accessibility, testing |
| 4 | `information-architect` | structure, navigation, labeling, cognitive load |
| 5 | `content-strategist` | messaging, persuasion, clarity, trust signals, microcopy (conversion-psychologist lane) |

**Craft skills on the shelf backing the pack:** `visual-design` · `design-systems` · `interaction-design` · `accessibility` · `prototyping-testing` · `anthropologist` · `information-architect` · `content-strategist`.
**Typical website build order:** anthropologist → information-architect → content-strategist → visual-design/design-systems → interaction-design + accessibility → prototyping-testing.

### Posting lane skills
`human-posting` (cookie-sync, human cadence, anti-bot, non-arrogant voice) · `social-content-posting` (pillars, hooks, captions, iteration) · `youtube-video-creation` (hooks, retention, thumbnails, SEO) · plus `caveman` + `agent-browser` from the base pack.

## HARD RULES (full list in SOUL.md §4)
No mock data · no secrets in code/logs/PRs · signed commits · never elevated · never `--no-verify`/`--force` · never global find-replace across deps · Square-only on youandinotai.com · zero FL §496.405 restricted vocabulary and no fundraising/cause/nonprofit framing on customer surfaces (Florida LLC, for-profit, product value only).
