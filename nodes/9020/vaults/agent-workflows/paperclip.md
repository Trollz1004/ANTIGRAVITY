# Paperclip

Current state on this node: installed, configured on port 3100, NOT running.

Role in the mesh: future backbone of the SCC — the execution layer that actually delivers approved posts to social channels. Longer term it connects onward to [[mission-control]] via the node-agent integration.
