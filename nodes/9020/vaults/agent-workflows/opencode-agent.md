# OpenCode agent

Current state on this node: INSTALLED, empty config — not yet doing work.

Role in the mesh: secondary coding/content platform once configured. Will take delegation from [[ornith-ceo]] and, like all agents here, submits drafts to the [[josh-approval-gate]] — it never posts on its own.
