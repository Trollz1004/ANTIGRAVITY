{
  "name": "mission-control-agency-swarm-v5",
  "version": "5.0.0",
  "private": true,
  "main": "electron/main.js",
  "description": "Mission Control: Agency Swarm v5 (Haiku-Sonnet 3.5 Edition) — orchestration dashboard for 148 specialized agents, routed through OmniRoute.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n SERVER,CLIENT -c cyan,white \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck",
    "gate": "npm --prefix server run typecheck && npm --prefix client run typecheck && npm --prefix server test && npm --prefix client test && npm --prefix server run role-wall",
    "test:e2e": "node scripts/require-runtime-gate.mjs && npm --prefix client run test:e2e",
    "electron": "electron .",
    "electron:dev": "concurrently -k -n SRV,CLI,EL -c cyan,white,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\" \"electron .\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "electron": "^43.2.0"
  },
  "dependencies": {
    "agent": "^0.0.4",
    "all": "^0.0.0",
    "dashboard": "^0.0.1",
    "hermes": "^0.0.8"
  }
}
