---
name: copilot-memory
last_session: 2026-09-11
total_sessions: 1
owner: copilot-cli (other agents: read-only, do not edit)
machine: alienware (192.168.0.40)
os: windows-11 (upgraded 2026-09-11)
gpu: amd-rx-6800-16gb
---

# Copilot Session Memory

> Copilot CLI only. Other agents: do not edit this file. Append at session end, read at session start.

## 2026-09-11 (Copilot CLI, Alienware node 192.168.0.40)

### Decisions
- Root-caused Ollama's "AMD driver is too old" warning: `detectOldAMDDriverWindows()` in ollama v0.34.0 (`discover/amd.go`) fires when `amdhip64_6.dll` is on PATH but `amdhip64_7.dll` (HIP 7 runtime) is missing. Confirmed: hip6 present in `System32`, hip7 absent. Installed driver (32.0.21045.5002, 2026-08-16) is recent but predates HIP 7.
- Machine upgraded to Windows 11 mid-session, so chose the full WHQL Adrenalin 26.9.1 (win11) package over the auto-detect web stub.
- Downloaded Adrenalin 26.9.1 (~986 MB) from AMD's official CDN; Authenticode Valid (Advanced Micro Devices, DigiCert-timestamped); installer launched and left waiting for the user's UAC/click-through.
- GPU inference already works meanwhile: `ollama serve` serves the RX 6800 (16 GiB) over Vulkan (`OLLAMA_VULKAN:true`), default ctx 4096.
- Cloned `Trollz1004/dream-online` to `C:\DREAM\dream-online` (HEAD `c4e225b`, public HTTPS clone, no credential prompt). Completes the clone half of the judge-lane carry-forward TODO.
- `.env` was NOT read or edited, per user reminder ("Claude has not edited that env yet... no paperclip"). Secrets were never echoed.

### Changes
- `CLAUDE.md` (Alienware row: Windows 10 -> Windows 11; session protocol now reads/writes the Copilot log; repo layout row updated), `docs/skills.md` (copilot-memory added to mandatory list), `skills/session-memory/memory.md` (judge-lane log: frontmatter node -> alienware, new block)
- This skill created: `skills/copilot-memory/SKILL.md`, `skills/copilot-memory/memory.md`
- Outside the repo: `C:\DREAM\dream-online` (new clone); `Downloads\whql-amd-software-adrenalin-edition-26.9.1-win11-a.exe`

### Lessons
- amd.com driver pages time out for plain fetches; `curl.exe` with a browser UA and referer works. `drivers.amd.com` direct links stall at 0 bytes without a UA/referer.
- "AMD driver is too old" in Ollama can mean "HIP 7 runtime DLL missing", not literally an old WDDM driver. Check `System32\amdhip64_6.dll` vs `amdhip64_7.dll` first.
- Native git stderr ("Cloning into...") shows as PowerShell RemoteException noise — cosmetic, not an error.
- amd.com's Windows 10 tab content loads via JS, so SSR HTML only exposes the win11/win7 links; OS-aware URL choice beats scraping.

### Carry-forward TODOs
- [ ] User approves + finishes the Adrenalin 26.9.1 install (installer window was open, PID 26236), reboots. After reboot: rerun `ollama serve`, confirm `amdhip64_7.dll` exists and the "driver too old" warning is gone (ROCm path available).
- [ ] Set `DREAM_ROOT` / `DREAM_REPO_DIR` in `.env` to `C:\DREAM\dream-online` — only with the user present.
- [ ] Park Paperclip in `.env` (comment out `PAPERCLIP_*` lines, keep values) — pending user go-ahead; do not do it silently.
- [ ] Remaining judge-lane TODOs live in `skills/session-memory/memory.md` (Obsidian vault, jarvis `NODE_LAN_IP` from `.env`, pnpm, GitHub sign-in + push, port clash 9219, README rewrite, docs sweep).